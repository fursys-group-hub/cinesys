// 이 파일을 복사해 같은 폴더에 config.js 로 저장하고, 따옴표 안에 실제 값을 채우세요.
// (config.js 는 git·배포 서버에 올라가지 않습니다 — 안전합니다.)
window.CINESYS_CONFIG = {
  FIREBASE_API_KEY: "",  // Firebase 웹 앱 설정의 apiKey
  TMDB_KEY: "",          // TMDB(영화 정보) API 키
  KOBIS_KEY: ""          // KOBIS(영화관 입장권) API 키
};
